{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ApplicationInsights": {
    "InstrumentationKey": "[The instrumentation key]" // Currently dont have access
  },
  "AzureKeyVaultEndpoint": "https://feebakpoc.vault.azure.net/",
  "AzureADManagedIdentityClientId": "" // Currently dont have this value , 
  // Way-1 For apps that use a user-assigned managed identity, configure the managed identity's Client ID.
  // Way-2 we can use clientId and secrets for accessing azure key vault

}
